#include "holberton.h"

/**
 * main - print the number of arguments passed into the program
 * @ac: number of arguments passed into the program
 * @av: array containing the arguments passed into the program
 *
 * Return: Always 0
 */

int main(int ac, char **av)
{
	(void) av;
	printf("%d\n", ac);
	return (0);
}
